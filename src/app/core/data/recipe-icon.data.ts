/**
 * Icône "recette" (wakassets/itemTypes/812.png, un parchemin) servie en
 * fichier statique (public/assets/ui/, nom hashé pour un cache navigateur
 * immuable — régénérer le hash si le fichier change). Affichée dans la liste
 * de résultats de l'autocomplétion pour tout objet `hasRecipe` (voir
 * wakfu-autocomplete.component).
 */
export const RECIPE_ICON_DATA_URI = '/assets/ui/recipe-b912eaf5.png';
